<IfModule mod_rewrite.c>
    RewriteEngine On
    # Route /amazingfeet/<path> into /public/<path>
    # Skip requests that are already under /public/
    RewriteRule ^$ public/ [L]
    RewriteRule ^((?!public/).*)$ public/$1 [L]
</IfModule>
